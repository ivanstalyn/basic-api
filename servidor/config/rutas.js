
/* Cofiguración de todas las rutas del componente*/

const rutas = {
  api: { url: '/api', def: '../routes/api' },
  tools: { url: '/tools', def: '../routes/tools' },
};

module.exports = { rutas };
